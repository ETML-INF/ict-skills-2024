import { TestRun } from '@skills17/test-result';
export default class Printer {
    private run;
    constructor(run: TestRun);
    /**
     * Prints the test run to the console in a nice format.
     */
    print({ printer, printHeader, printFooter, printPoints, }?: {
        printer?: (...data: string[]) => void;
        printHeader?: boolean;
        printFooter?: boolean;
        printPoints?: boolean;
    }): void;
    private getPointsColor;
    private printTotal;
    private printWarnings;
    private printTestWarning;
}
