export default class Override {
    private match;
    private required;
    private points?;
    private pattern;
    constructor(match: string, required?: boolean, points?: number | undefined);
    /**
     * Check if a test matches this override
     *
     * @param match Test name to check
     */
    matches(match: string): boolean;
    getPoints(): number | undefined;
    isRequired(): boolean;
}
