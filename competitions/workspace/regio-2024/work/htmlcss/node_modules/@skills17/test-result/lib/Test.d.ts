export default class Test {
    private name;
    private maxPoints;
    private successful;
    private required;
    private manualCheck;
    constructor(name: string, maxPoints: number, successful: boolean, required: boolean);
    getName(): string;
    getPoints(): number;
    getMaxPoints(): number;
    isSuccessful(): boolean;
    isRequired(): boolean;
    setManualCheck(manualCheck: boolean): void;
    requiresManualCheck(): boolean;
    toJSON(): Record<string, unknown>;
}
