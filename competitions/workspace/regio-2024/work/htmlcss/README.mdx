import { Highlight } from "@skills17/competitor-readme";

# Task 1 - CSS Selectors

Select the **Swiss Flag** 🇨🇭.

- Edit the file [src/task.css](http://localhost:4001/htmlcss/src/task.css) with your editor and only modify the selectors.
- Look at the HTML in [src/task.html](http://localhost:4001/htmlcss/src/task.html) to see the structure and look
  at it in a browser to see the result.

`#task-0` is provided to you as an example.

## General Information

Your working space is the folder `work/htmlcss/`.

<Highlight type="error">
  You can only change the file `work/htmlcss/src/task.css`. All other
  files will be reverted for the review process.
</Highlight>

## Rules

1. You are only allowed to use **1 selector per task**:
   - Allowed: `#task-1 .switzerland {}`
   - Not allowed: `#task-1 .germany, #task-1 .switzerland {}`
2. The selector **must** begin with the task ID eg. `#task-1`
3. You are **not allowed** to edit the HTML.
4. You are **not allowed** to use the `data-target` attriubte for your selector.
5. You are **not allowed** to use any of the following selectors:
   - `:nth-child`
   - `:nth-last-child`
   - `:nth-of-type`
   - `:nth-last-of-type`
   - any use of `+` or `~`

## Available files

| Description                                                                                       | File                                                         |
|---------------------------------------------------------------------------------------------------|--------------------------------------------------------------|
| HTML file: **don't edit this** but use it to see your tasks and if the your selectors are correct | [src/task.html](http://localhost:4001/htmlcss/src/task.html) |
| CSS file: you work here                                                                           | [src/task.css](http://localhost:4001/htmlcss/src/task.css)   |
| Unit tests to check if your selectors are correct                                                 | [tests.html](http://localhost:4001/htmlcss/tests.html)       |

<br />
